/**
 * Flip to true to take the whole site down for everyone (shows a "down for updates"
 * page instead of the gate/library) while you edit without anyone reading mid-draft.
 * Flip back to false and redeploy when ready to publish again.
 */
export const MAINTENANCE_MODE = false;
